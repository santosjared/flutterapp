import 'package:flutter/material.dart';

class colorScheme {
  static const lightBackground = Color(0xFFfdfffc);
  static const primary = Color.fromARGB(255, 0, 155, 80);

  static const darkBackground = Color.fromARGB(255, 56, 65, 80);

  static const textLight = Colors.black;
  static const textDark = Colors.white;
}